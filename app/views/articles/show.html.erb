<h2><%= @article.title %></h2>
<p><%= @article.content %></p>
<%= link_to "Delete quote",
            article_path(@article),
            method: :delete,
            data: { confirm: "Are you sure?"} %>
